{
  "training_rows_policy": "use_all_rows",
  "training_rows_rule": null,
  "split_column": null,
  "metric_policy": {
    "primary_metric": "auc_roc",
    "report_with_cv": true,
    "notes": "Inferred from predictive strategy type (propensity modeling) and contract requirements for AUC_ROC."
  },
  "cv_policy": {
    "strategy": "StratifiedKFold",
    "n_splits": 5,
    "shuffle": true,
    "notes": "Standard for binary classification to ensure target class distribution is preserved across folds."
  },
  "leakage_policy": {
    "action": "none",
    "flagged_columns": [],
    "notes": "No leakage flags found in data profile. Runtime audit per contract (correlation_with_target) will be executed."
  },
  "evidence": [
    "outcome_analysis is empty (no evidence of >1% missing labels)",
    "split_candidates is empty (no split column identified)",
    "leakage_flags is empty (no pre-flagged columns)",
    "strategy analysis_type is 'predictive'",
    "contract requires 'auc_roc' in metrics.json"
  ],
  "assumptions": [
    "Data profile was empty, assuming standard supervised learning setup with complete labels.",
    "Target variable is binary based on business objective (propensity to convert)."
  ],
  "open_questions": [
    "Why is the data profile empty? (Proceeding with defaults)"
  ]
}