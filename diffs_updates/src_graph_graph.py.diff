diff --git a/src/graph/graph.py b/src/graph/graph.py
index 8d8ade4..21734a3 100644
--- a/src/graph/graph.py
+++ b/src/graph/graph.py
@@ -2765,17 +2765,20 @@ def _filter_missing_required_constants(
 def _resolve_contract_deliverables(contract: Dict[str, Any]) -> List[Dict[str, Any]]:
     """
     V4.1 compatible deliverables resolver.
-    Returns a list of dicts [{'path': str, 'required': bool, ...}]
+    Returns a list of dicts [{'path': str, 'required': bool, 'kind': str, 'owner': str, ...}]
     """
-    from src.utils.contract_accessors import get_required_outputs
+    from src.utils.contract_accessors import get_deliverables, get_required_outputs
 
-    # V4.1: get_required_outputs returns List[str]
+    # Prefer rich deliverables (with kind/owner metadata)
+    deliverables = get_deliverables(contract)
+    if deliverables:
+        return deliverables
+
+    # Fallback: get_required_outputs returns List[str]
     outputs = get_required_outputs(contract)
     if outputs:
-        # Convert to list of dicts for compatibility with existing graph logic
         return [{"path": p, "required": True} for p in outputs]
 
-    # V4.1: No legacy spec_extraction fallback
     return []
 
 
@@ -3693,20 +3696,8 @@ def _deliverable_id_from_path(path: str) -> str:
     return cleaned or "deliverable"
 
 def _infer_deliverable_kind(path: str) -> str:
-    lower = str(path).lower()
-    if "plots" in lower or lower.endswith((".png", ".jpg", ".jpeg", ".svg")):
-        return "plot"
-    if lower.endswith(".csv"):
-        return "dataset"
-    if lower.endswith(".json"):
-        if "metrics" in lower:
-            return "metrics"
-        if "weights" in lower:
-            return "weights"
-        if "alignment" in lower or "report" in lower:
-            return "report"
-        return "json"
-    return "artifact"
+    from src.utils.contract_accessors import _infer_kind_from_path
+    return _infer_kind_from_path(path)
 
 def _infer_scored_rows_schema(
     contract: Dict[str, Any],
